#include "TextRenderer.h"

Text::Text(IRenderer& renderer)
{
    // load the textures
}

std::vector<std::optional<Texture>> Text::getCharacterTextures(const char* text)
{
    // for each character in the text, add to the vector the corresponding texture
}